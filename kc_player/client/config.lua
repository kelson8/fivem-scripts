PlayerConfig = {
    -- This seems to work for the spawn coords. 
    -- Changing these will change the location of the /spawn command.
    SpawnX = 222.2027,
    SpawnY = -864.0162,
    SpawnZ = 29.2922,
}

-- I don't know if this'll work.
-- SpawnVector = vector3(PlayerConfig.SpawnX, playerConfig.SpawnY, PlayerConfig.SpawnZ)
